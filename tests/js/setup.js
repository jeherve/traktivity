/**
 * WordPress dependencies
 */

// Fails a test if it logs to the console without asserting that it would.
import '@wordpress/jest-console';

/**
 * External dependencies
 */
import '@testing-library/jest-dom';
