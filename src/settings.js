/**
 * Values handed to the dashboard from PHP by wp_localize_script().
 *
 * Reading it once here keeps the global out of every component, and gives
 * tests a single place to stub.
 */
export default window.traktivity_dash || {};
